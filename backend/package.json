{
  "name": "lks-backend",
  "version": "0.1.0",
  "description": "Local development backend for the LuftKraftSport Android app",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "reset": "rm -f data/lks.db && rm -rf data/uploads/* && node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  }
}
