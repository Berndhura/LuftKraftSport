<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:camper="http://schemas.android.com/tools">


    <item
        android:id="@+id/refresh"
        android:icon="@drawable/notification_template_icon_bg"
        android:title="Refresh"
        camper:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/abc_ic_search"
        android:title="@string/abc_search_hint"
        camper:actionViewClass="android.widget.SearchView"
        camper:showAsAction="always|collapseActionView" />


    <item
        android:id="@+id/login"
        android:icon="@drawable/ic_action_settings"
        android:title="Login"
        camper:showAsAction="never" />

    <item
        android:id="@+id/profile"
        android:icon="@drawable/ic_action_settings"
        android:title="Profile"
        camper:showAsAction="never" />

</menu>
